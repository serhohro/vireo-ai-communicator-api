[pytest]
testpaths = tests
python_files = test_*.py
asyncio_mode = auto
pythonpath = .
markers =
    slow: slow tests
    interop: cross-language interoperability tests
    bench: benchmark tests
