name: Vireo Benchmark

on:
  workflow_dispatch:
  schedule:
    - cron: "0 3 * * 0"

jobs:
  bench:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install -r requirements.txt
      - name: Wire benchmark
        run: python -m benches.wire_perf.bench_wire --profile=all
      - name: Crypto benchmark
        run: python -m benches.wire_perf.bench_crypto